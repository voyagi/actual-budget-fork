# UAT Audit Report - 2026-05-06

Generated by `/gsd-audit-uat --auto`; updated after fixing actionable findings.

## Summary

Current scanner output shows **3 outstanding UAT/runtime groups across 2 files**:

- 1 `human_uat` group that requires an Android device.
- 2 `third_party` groups for production real-bank Enable Banking OAuth/sync, intentionally deferred because this environment uses sandbox banking.

The original audit under-reported the backlog because `gsd-sdk query audit-uat --raw` only returned Phase 04. I fixed the scanner so it:

- scans all phase directories instead of milestone-filtering,
- parses UAT blocks with blank lines,
- reads `human_verification` frontmatter,
- extracts numbered human-verification headings instead of nested expected-result bullets,
- categorizes skipped external-provider tests as `third_party`.

Current environment notes:

- `https://actual-budget.taranity.com/health` returned `{"status":"UP"}` on 2026-05-06.
- `docker-compose ps` shows Caddy and Cloudflared up, and the rebuilt sync-server container healthy on 2026-05-06.
- Project memory says Phase 5 production deployment is sandbox-backed and real-bank Enable Banking OAuth/sync is intentionally deferred outside Phase 5.

## Fixed Items

| Area                                 | Fix                                                                                                                                | Evidence                                                                                                                                       |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| UAT scanner                          | Fixed scanner coverage and parsing so the CLI now reports all active UAT/human-verification groups.                                | `gsd-sdk query audit-uat --raw` now reports 3 remaining groups across Phases 04 and 05.                                                        |
| Phase 05.2 password hardening        | Added automated coverage for bootstrap/change password minimums and legacy short-password login.                                   | `packages/sync-server/src/accounts/password.test.ts`; targeted sync-server test passed 10/10 with OpenID tests.                                |
| Phase 05.2 OpenID redirect hardening | Added automated coverage for exact scheme/host/port redirect matching.                                                             | `packages/sync-server/src/accounts/openid.test.js`; targeted sync-server test passed.                                                          |
| Phase 05.2 rule regex safety         | Added tests for literal special-character replacements and dangerous nested-quantifier regex literals.                             | `packages/loot-core/src/server/rules/index.test.ts`; targeted loot-core test passed 111/111.                                                   |
| Rule helper bug                      | Fixed non-regex `replace`/`replaceAll` so escaped special-character search strings are used through safe `RegExp` objects.         | `packages/loot-core/src/server/rules/handlebars-helpers.ts`; tests cover `McDonald's (Drive-Thru)`, `$5 Store`, and `/(a+)+$/`.                |
| Phase 05.2 encryption UAT state      | Marked PBKDF2 upgrade checks complete based on existing encryption tests.                                                          | `packages/loot-core/src/server/encryption/encryption.test.ts` verifies 100,000-iteration metadata and 10,000-iteration backward compatibility. |
| Phase 04 iOS state                   | Replaced stale "accept iOS deferral" language with the real iOS Safari install smoke test now that Cloudflare Tunnel HTTPS exists. | `.planning/phases/04-pwa-completion/04-VERIFICATION.md` now tracks iOS install/standalone launch as `human_needed`.                            |
| Phase 04 iOS install                 | Closed iOS Safari install and standalone-mode human verification.                                                                  | User reported `iOS install pass` on 2026-05-06.                                                                                                |
| Phase 04 offline read                | Closed installed-PWA offline read human verification.                                                                              | User reported `offline read pass` on 2026-05-06.                                                                                               |
| Phase 09 TOTP runtime UAT            | Waived 2FA/TOTP browser smoke tests without marking them passed.                                                                   | User said they do not care about 2FA on 2026-05-06.                                                                                            |
| Phase 09 backup Docker layout        | Fixed server backup to support Docker production paths and clean failed working directories.                                       | `packages/sync-server/src/util/backup.ts`; live container `runBackup()` created `/data/backups/backup-2026-05-06T16-45-39.tar.gz`.             |
| Phase 09 backup disk status          | Fixed "Backups on disk" to count real `.tar.gz` archives instead of only an in-memory process counter.                             | `getBackupStatus()` in the rebuilt container reported disk count 1 before backup and 2 after backup.                                           |
| Phase 09 backup UI status            | Fixed backup status IPC unwrapping/no-cache behavior and confirmed browser-visible Settings status updates.                        | User confirmed Settings shows last backup `5/6/2026, 7:08:44 PM`, `Status: Success`, `Size: 55.9 KB`, and `Backups on disk: 4`.                |

## Remaining Items

### Phase 04: PWA Runtime

| #   | Test                                    | Why Still Manual                                                    |
| --- | --------------------------------------- | ------------------------------------------------------------------- |
| 1   | Android PWA install and standalone mode | Requires Android Chrome install prompt/home-screen launch behavior. |

Android status: not attempted on 2026-05-06 because the user does not have an Android phone available.

### Phase 05: Production Real-Bank Follow-Up

| #   | Test                       | Status                                                                                          |
| --- | -------------------------- | ----------------------------------------------------------------------------------------------- |
| 10  | Production real-bank OAuth | Skipped; production Enable Banking application and real bank account are not configured.        |
| 11  | Production real-bank sync  | Skipped; production Enable Banking application and real linked bank account are not configured. |

### Phase 09: Backup Runtime

Status: complete on 2026-05-06. The rebuilt Docker sync-server is healthy; direct `runBackup()` created a valid `.tar.gz` archive from `/data`; `getBackupStatus()` now reads real disk archive count; logs show auto-backup scheduled with `0 2 * * *`; user confirmed the browser Settings panel shows backup success, timestamp, size, and disk count.

TOTP runtime status: user-waived on 2026-05-06 because the user does not care about 2FA. Static and unit test coverage still passed.

## Verification Commands

Passed from the repo root:

- `yarn workspace @actual-app/sync-server test src/accounts/password.test.ts src/accounts/openid.test.js`
- `yarn workspace loot-core test:node src/server/rules/index.test.ts src/server/encryption/encryption.test.ts`
- `yarn workspace @actual-app/sync-server test src/accounts/totp.test.ts src/util/backup.test.ts`
- `yarn workspace @actual-app/sync-server test src/util/backup.test.ts`
- `yarn oxlint --type-aware packages/sync-server/src/util/backup.ts packages/sync-server/src/util/backup.test.ts`
- `yarn oxfmt --check packages/sync-server/src/util/backup.ts packages/sync-server/src/util/backup.test.ts`
- `yarn workspace @actual-app/sync-server build`
- `docker-compose build sync-server`
- `docker-compose up -d --no-deps sync-server`
- `docker-compose exec -T sync-server node -e "... runBackup() ... getBackupStatus() ..."`
- `gsd-sdk query audit-uat --raw`
- `curl.exe -sS --max-time 10 https://actual-budget.taranity.com/health`
- `docker-compose ps`

## Prioritized Human Test Plan

1. Run Android PWA install later if an Android device becomes available.
2. Leave Phase 05 production real-bank OAuth/sync deferred until a production Enable Banking app and real bank credentials exist.
