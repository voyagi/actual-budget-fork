---
status: complete
phase: 04.1-audit-quick-wins
source: [04.1-01-SUMMARY.md, 04.1-02-SUMMARY.md, 04.1-03-SUMMARY.md]
started: 2026-03-03T18:39:00Z
updated: 2026-03-03T18:47:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Consent banner urgency icons
expected: Consent banners show distinct SVG icons per urgency level - info circle for "soon", exclamation outline for "urgent", filled exclamation for "expired" - before the message text
result: pass

### 2. Consent banner dismiss button
expected: The dismiss button (X) on consent banners shows a small X icon (SvgDelete) instead of the plain text times character
result: pass

### 3. Calendar keyboard focus ring
expected: Open a date picker, Tab to calendar day buttons. A 2px ring appears around the focused day for keyboard navigation. Mouse clicking a day does NOT show the ring.
result: pass

### 4. Skip navigation link
expected: Press Tab from the top of any page. A "Skip to main content" link appears at top-left. Activating it moves focus past the sidebar to the main content area.
result: pass

### 5. Re-authorize button design system styling
expected: Navigate to an account needing re-authorization. The "Re-authorize" link should show hover highlighting and a focus ring when tabbed to (design system Button behavior, not a raw HTML link).
result: pass

### 6. Enable Banking modal responsive width
expected: Open the Enable Banking authorization flow modal. On a standard desktop viewport it should be ~30% wide (not filling the screen). Narrowing the browser below ~400px should keep the modal at a readable minimum width.
result: pass

### 7. American English spelling in modal
expected: The Enable Banking modal text says "authorize access" and "authorization" (American English), not "authorise"/"authorisation" (British English).
result: pass

### 8. Production build minification (terser)
expected: Running a production build (`yarn build`) produces minified JavaScript bundles. Variable names are mangled and whitespace is compressed (no human-readable unminified output).
result: pass

### 9. Security headers on sync-server responses
expected: When running the sync-server in non-development mode, HTTP responses include Strict-Transport-Security and Content-Security-Policy headers. (Requires running server with NODE_ENV=production)
result: pass

### 10. Auth rate limiting
expected: After 5 failed login attempts to /account/login within 15 minutes, the 6th attempt returns HTTP 429 with a "too-many-requests" error message. (Requires running server)
result: pass

### 11. Dockerfile runs as non-root
expected: Building and running the Docker container, the process runs as user "node" (not root). Verifiable with `docker exec <container> whoami` returning "node".
result: pass

### 12. CORS origin restriction
expected: The sync-server rejects cross-origin requests from origins other than ACTUAL_CORS_ORIGIN. Requests from non-whitelisted origins get no Access-Control-Allow-Origin header. (Requires running server)
result: pass

### 13. JSON Content-Type on file access errors
expected: File access error responses (403) from the sync-server include Content-Type: application/json header (not text/html or text/plain). (Requires running server)
result: pass

## Summary

total: 13
passed: 13
issues: 0
pending: 0
skipped: 0

## Gaps

[none]
