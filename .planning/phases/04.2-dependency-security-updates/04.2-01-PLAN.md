---
phase: 04.2-dependency-security-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - yarn.lock
autonomous: true
requirements:
  - sec-8
  - sec-9
  - sec-10
  - sec-11
  - sec-12
  - sec-13
  - sec-14
must_haves:
  truths:
    - "trivy fs . reports no findings for tar, rollup, serialize-javascript, storybook, jws, axios, or lodash CVEs (sec-8 through sec-14)"
    - "yarn build completes without errors after all dependency updates"
    - "yarn test passes after all dependency updates"
    - "yarn typecheck passes after all dependency updates"
  artifacts:
    - path: "package.json"
      provides: "Updated resolutions section with rollup, jws, serialize-javascript, glob, ajv overrides"
      contains: "serialize-javascript"
    - path: "yarn.lock"
      provides: "Refreshed lockfile with all CVE-affected packages at safe versions"
  key_links:
    - from: "package.json resolutions"
      to: "yarn.lock resolved versions"
      via: "yarn install re-resolution"
      pattern: "serialize-javascript.*7\\.0\\.3"
    - from: "yarn.lock"
      to: "trivy scan results"
      via: "trivy fs . reads yarn.lock"
      pattern: "rollup.*4\\.59"
---

<objective>
Remediate all trivy-flagged CVEs (sec-8 through sec-14) in the yarn.lock by refreshing the lockfile for direct dep CVEs, adding resolution overrides for transitive dep CVEs, and updating the rollup pin. Verify build, test, and typecheck pass after each update group.

Purpose: Eliminate known security vulnerabilities in project dependencies before production deployment.
Output: Updated package.json resolutions + clean yarn.lock with no sec-8 through sec-14 CVEs remaining.
</objective>

<execution_context>
@C:/Users/Eagi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eagi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-dependency-security-updates/04.2-CONTEXT.md
@.planning/phases/04.2-dependency-security-updates/04.2-RESEARCH.md

<interfaces>
<!-- Current resolutions section in root package.json -->
```json
"resolutions": {
  "@codemirror/view": ">=6.39.15",
  "@isaacs/brace-expansion": ">=5.0.1",
  "qs": ">=6.14.1",
  "rollup": "4.40.1",
  "socks": ">=2.8.3"
}
```

<!-- CVE-to-package mapping (from RESEARCH.md) -->
sec-8:  tar         -> 7.5.8+  (remote master has 7.5.9 via Dependabot)
sec-9:  rollup      -> 4.59.0+ (update resolution pin from 4.40.1)
sec-10: serialize-js -> 7.0.3+ (add resolution override)
sec-11: storybook   -> 10.2.10+ (remote master has 10.2.13 via Dependabot)
sec-12: jws 3.x     -> 3.2.3+  (add resolution override; 4.x already safe)
sec-13: axios       -> 1.13.5+ (lockfile refresh; add resolution if 1.12.2 persists)
sec-14: lodash      -> 4.17.23 (lockfile refresh)

<!-- Additional MEDIUM CVEs from phase goal -->
glob -> 10.5.0  (add resolution override)
ajv  -> 8.18.0  (add resolution override)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lockfile refresh and direct dep CVE resolution</name>
  <files>yarn.lock</files>
  <action>
Refresh the yarn lockfile to pick up already-declared safe version ranges for direct dependencies. This resolves sec-8 (tar), sec-11 (storybook), sec-13 (axios), and sec-14 (lodash) without any package.json changes.

Steps:
1. Fetch latest remote master: `git fetch origin master`
2. Merge remote master into current branch: `git merge origin/master --no-edit`
   - This inherits Dependabot commits that updated tar to 7.5.9 and storybook to 10.2.13 in yarn.lock
   - If merge conflicts occur in yarn.lock, accept the incoming (master) version and run `yarn install --refresh-lockfile` to reconcile
3. After merge, run `yarn install --refresh-lockfile` to refresh the lockfile within declared ranges (per CONTEXT.md locked decision)
   - axios: sync-server declares ^1.13.5, should resolve 1.12.2 -> 1.13.5+
   - lodash: desktop-client declares ^4.17.23, should resolve 4.17.21 -> 4.17.23
4. Verify resolved versions in yarn.lock:
   - `grep -A2 '"tar@npm' yarn.lock` -- expect version >= 7.5.8
   - `grep -A2 '"axios@npm' yarn.lock` -- check if 1.12.2 entry is gone
   - `grep -A2 '"lodash@npm' yarn.lock` -- check if 4.17.21 entry is gone
   - `grep '"storybook@npm' yarn.lock` -- expect version >= 10.2.10
5. If axios 1.12.2 persists after refresh (Pitfall 4 from RESEARCH.md: a transitive dep like nordigen-node declares ^1.2.1), note it for Task 2 where it will be added to resolutions
6. Run full CI suite: `yarn build && yarn test && yarn typecheck && npx oxfmt --check packages/sync-server/src/app-enablebanking/ packages/sync-server/src/scheduler.ts packages/desktop-client/src/components/ConsentExpiryBanner.tsx packages/desktop-client/src/components/FinancesApp.tsx packages/desktop-client/src/hooks/useEnableBankingStatus.ts packages/loot-core/src/server/accounts/provider-status.ts`
7. Commit: `chore(deps): merge remote master + lockfile refresh for CVE remediation`

Expected CVEs resolved by this task:
- sec-8 (tar): resolved via Dependabot merge
- sec-11 (storybook): resolved via Dependabot merge
- sec-13 (axios): resolved via lockfile refresh (or flagged for resolution override)
- sec-14 (lodash): resolved via lockfile refresh
  </action>
  <verify>
    <automated>! (grep -A5 '"tar@npm' yarn.lock | grep -q "version: 7\.5\.[0-7]$") && grep -q "version: 4\.17\.23" yarn.lock && yarn build > /dev/null 2>&1</automated>
  </verify>
  <done>Remote master merged. yarn.lock contains tar >= 7.5.8, storybook >= 10.2.10, lodash 4.17.23. Axios 1.12.2 either resolved or documented for resolution override. yarn build + test + typecheck + oxfmt all pass.</done>
</task>

<task type="auto">
  <name>Task 2: HIGH severity CVE remediation - rollup, serialize-javascript, jws</name>
  <files>package.json, yarn.lock</files>
  <action>
Add resolution overrides for HIGH severity transitive CVEs and update the rollup pin. This resolves sec-9 (rollup RCE), sec-10 (serialize-javascript RCE), and sec-12 (jws HMAC bypass).

Steps:
1. Edit root package.json `resolutions` section. Update existing rollup entry and add new entries:
   - Change `"rollup": "4.40.1"` to `"rollup": ">=4.59.0 <5.0.0"`
     (Range keeps deduplication purpose while allowing Dependabot bumps. Upper bound prevents accidental rollup 5.x which has breaking changes. Per RESEARCH.md: rollup 4.40.1->4.59.0 has no breaking changes, vite 7.3.1 requires ^4.43.0 anyway)
   - Add `"jws": ">=3.2.3"` (forces transitive 3.x consumers to patched version; direct 4.x dep already at safe 4.0.1)
   - Add `"serialize-javascript": ">=7.0.3"` (forces transitive consumers from 6.0.2 to patched version)
   - If axios 1.12.2 persisted from Task 1, also add `"axios": ">=1.13.5"`

2. Run `yarn install` to apply resolution overrides

3. Verify in yarn.lock:
   - `grep -A2 '"rollup@npm' yarn.lock` -- expect version >= 4.59.0
   - `grep -A2 '"jws@npm:' yarn.lock` -- expect NO 3.2.2 entries, only 3.2.3+ and 4.0.1+
   - `grep -A2 '"serialize-javascript@npm' yarn.lock` -- expect version >= 7.0.3
   - If serialize-javascript 6.0.2 persists (Pitfall 5 from RESEARCH.md), switch to exact pin `"serialize-javascript": "7.0.3"` and re-run yarn install

4. Run `yarn dedupe` to collapse any redundant lockfile entries

5. Run full CI suite: `yarn build && yarn test && yarn typecheck && npx oxfmt --check packages/sync-server/src/app-enablebanking/ packages/sync-server/src/scheduler.ts packages/desktop-client/src/components/ConsentExpiryBanner.tsx packages/desktop-client/src/components/FinancesApp.tsx packages/desktop-client/src/hooks/useEnableBankingStatus.ts packages/loot-core/src/server/accounts/provider-status.ts`
   - If build fails with rollup plugin errors, check whether a rollup plugin is incompatible with 4.59.0 and fix forward per CONTEXT.md breakage rule

6. Commit: `fix(sec): remediate HIGH severity CVEs - rollup RCE, serialize-js RCE, jws HMAC bypass`

Expected CVEs resolved by this task:
- sec-9 (rollup CVE-2026-27606): resolved via resolution pin update
- sec-10 (serialize-javascript GHSA-5c6j-r48x-rmvq): resolved via resolution override
- sec-12 (jws CVE-2025-65945 3.x path): resolved via resolution override
- sec-13 (axios, if persisted): resolved via resolution override
  </action>
  <verify>
    <automated>grep "serialize-javascript" yarn.lock | grep -v "6.0" && grep "rollup@" yarn.lock | head -3 && ! (grep -A5 '"jws@npm' yarn.lock | grep -q "version: 3\.2\.2")</automated>
  </verify>
  <done>package.json resolutions updated with rollup >=4.59.0 <5.0.0, jws >=3.2.3, serialize-javascript >=7.0.3. yarn.lock reflects patched versions. No 3.2.2 jws or 6.0.2 serialize-javascript entries remain. Build + test + typecheck + oxfmt all pass.</done>
</task>

<task type="auto">
  <name>Task 3: MEDIUM severity CVE remediation + trivy final gate</name>
  <files>package.json, yarn.lock</files>
  <action>
Add resolution overrides for MEDIUM severity transitive CVEs (glob, ajv) and run the trivy final gate to confirm all sec-8 through sec-14 are resolved.

Steps:
1. Edit root package.json `resolutions` section. Add:
   - `"glob": ">=10.5.0"` (fixes CVE-2025-64756 command injection in CLI)
   - `"ajv": ">=8.18.0"` (fixes CVE-2025-69873 ReDoS via $data)

2. Run `yarn install` to apply resolution overrides

3. Verify in yarn.lock:
   - `grep -A2 '"glob@npm' yarn.lock` -- expect version >= 10.5.0, no 10.4.5 entries
   - `grep -A2 '"ajv@npm' yarn.lock` -- expect version >= 8.18.0, no 8.17.1 entries

4. Run `yarn dedupe` to collapse any redundant lockfile entries

5. Run full CI suite: `yarn build && yarn test && yarn typecheck && npx oxfmt --check packages/sync-server/src/app-enablebanking/ packages/sync-server/src/scheduler.ts packages/desktop-client/src/components/ConsentExpiryBanner.tsx packages/desktop-client/src/components/FinancesApp.tsx packages/desktop-client/src/hooks/useEnableBankingStatus.ts packages/loot-core/src/server/accounts/provider-status.ts`
   - If glob 10.5.0 causes issues with older consumers, check if the resolution breaks brace patterns. Fix forward or pin a known-good version per CONTEXT.md breakage rule.

6. Run `trivy fs . --severity HIGH,CRITICAL --exit-code 1` as final gate
   - If trivy exits non-zero, inspect findings against sec-8 through sec-14 requirement IDs
   - For each requirement, confirm the CVE is no longer reported OR document it as "accepted risk" with reason
   - Minimatch 3.x CVEs (CVE-2026-26996/27903/27904) may remain as unfixable transitive deps in build tooling. Per RESEARCH.md: "attack requires attacker-controlled regex patterns in build toolchain - limited practical risk." Document as accepted risk if present.

7. Commit: `fix(sec): remediate MEDIUM severity CVEs - glob command injection, ajv ReDoS`

8. If any sec-8 through sec-14 CVEs remain after trivy scan, document them with:
   - CVE ID
   - Why it cannot be resolved (transitive dep, no safe version in range, would break build)
   - Risk assessment (build-only, dev-only, or production-facing)

Expected CVEs resolved by this task:
- glob (phase goal): resolved via resolution override
- ajv (phase goal): resolved via resolution override
- Final verification of all sec-8 through sec-14
  </action>
  <verify>
    <automated>grep "ajv" yarn.lock | grep -v "8.17" | head -3 && trivy fs . --severity HIGH,CRITICAL --exit-code 1</automated>
  </verify>
  <done>package.json resolutions include glob >=10.5.0 and ajv >=8.18.0. yarn.lock reflects patched versions. trivy fs . shows no findings for sec-8 through sec-14 packages (or any remaining CVEs are documented as accepted risk with justification). Build + test + typecheck + oxfmt all pass.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `trivy fs .` shows clean results for all sec-8 through sec-14 packages
2. `yarn build` succeeds (no dependency breakage from version bumps)
3. `yarn test` passes (no behavioral regressions)
4. `yarn typecheck` passes (no type incompatibilities from version changes)
5. `grep -c "serialize-javascript\|rollup\|jws\|glob\|ajv" package.json` confirms all resolution overrides are present in package.json
6. Any remaining trivy findings are documented as accepted risk with justification

Verification command:
```bash
yarn build && yarn test && yarn typecheck && trivy fs . --severity HIGH,CRITICAL --exit-code 1
```
</verification>

<success_criteria>
- All 7 requirement IDs (sec-8 through sec-14) are addressed: either the CVE is resolved in yarn.lock or documented as accepted risk
- Zero HIGH/CRITICAL trivy findings for the 7 target packages (tar, rollup, serialize-javascript, storybook, jws, axios, lodash)
- Build, test, and typecheck all pass on the final commit
- package.json resolutions section contains the new override entries
- No unnecessary resolution overrides added (if lockfile refresh resolved a CVE, no redundant pin exists)
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-dependency-security-updates/04.2-01-SUMMARY.md`
</output>
