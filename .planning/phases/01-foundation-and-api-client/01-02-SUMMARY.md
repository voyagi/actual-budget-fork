---
phase: 01-foundation-and-api-client
plan: 02
subsystem: infra
tags: [enable-banking, rsa, pem, secrets, sandbox]

# Dependency graph
requires: []
provides:
  - "RSA private key at secrets/eb_private.pem (PKCS#8 format, sandbox)"
  - ".env with ENABLE_BANKING_APP_ID set to real sandbox application ID"
  - "Enable Banking sandbox application registered at enablebanking.com/cp"
affects:
  - "01-03 (JWT signing and API client need the private key and app ID)"
  - "all Enable Banking API calls through all phases"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "secrets/ directory for RSA keys mounted into Docker as read-only volume"
    - ".env gitignored, .env.example committed for onboarding"

key-files:
  created:
    - "secrets/eb_private.pem (gitignored, not committed)"
    - ".env (gitignored, not committed)"
  modified: []

key-decisions:
  - "Application ID b619fe6c-ab92-4de5-a7c2-901c0e0ef580 is the sandbox app (not production)"
  - "Redirect URL set to http://localhost:5006/enablebanking/callback to match planned Express route"
  - "Key format is PKCS#8 (BEGIN PRIVATE KEY) not PKCS#1 (BEGIN RSA PRIVATE KEY) - jose handles both"

patterns-established:
  - "Sandbox RSA key: PKCS#8 format from Enable Banking browser-generated key pair"
  - "Secret storage: secrets/ directory gitignored, Docker mount as :ro file (not env var)"

requirements-completed: [FOUND-03]

# Metrics
duration: 15min
completed: 2026-02-18
---

# Phase 01 Plan 02: Enable Banking Sandbox Registration Summary

**Enable Banking sandbox application registered with PKCS#8 RSA private key at secrets/eb_private.pem and app ID b619fe6c-ab92-4de5-a7c2-901c0e0ef580 configured in .env**

## Performance

- **Duration:** ~15 min (manual human action)
- **Started:** 2026-02-18
- **Completed:** 2026-02-18
- **Tasks:** 1/1
- **Files modified:** 0 (secrets not committed)

## Accomplishments

- Enable Banking sandbox application registered at enablebanking.com/cp
- RSA private key (PKCS#8 format) placed at secrets/eb_private.pem
- .env configured with real sandbox application ID
- Both secrets/eb_private.pem and .env confirmed gitignored - neither will be committed
- Redirect URL http://localhost:5006/enablebanking/callback configured on the application

## Task Commits

This plan was entirely a human-action checkpoint. No code was committed.

- **Task 1: Register Enable Banking sandbox application and download RSA key** - Human action, no commit

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `secrets/eb_private.pem` - RSA private key for Enable Banking JWT signing (gitignored, PKCS#8 format)
- `.env` - Enable Banking sandbox configuration with real app ID (gitignored)

Note: Neither file is committed to git. They exist only on the local filesystem.

## Decisions Made

- Redirect URL set to http://localhost:5006/enablebanking/callback - matches the Express route planned for Plan 01-03
- Key is PKCS#8 format (BEGIN PRIVATE KEY header) which jose handles natively - no format conversion needed

## Deviations from Plan

None - plan executed exactly as designed. This plan was a single human-action checkpoint by design.

## Issues Encountered

None.

## User Setup Required

**This entire plan was user setup.** The following was completed manually:

- Registered sandbox application at [enablebanking.com/cp](https://enablebanking.com/cp)
- Downloaded RSA private key generated by Enable Banking browser UI
- Placed key at `secrets/eb_private.pem`
- Copied `.env.example` to `.env` and set `ENABLE_BANKING_APP_ID=b619fe6c-ab92-4de5-a7c2-901c0e0ef580`

## Next Phase Readiness

Plan 01-03 (JWT authentication and API client) is fully unblocked:

- RSA private key available at `secrets/eb_private.pem` for JWT RS256 signing
- Application ID known: `b619fe6c-ab92-4de5-a7c2-901c0e0ef580`
- Sandbox redirect URL configured: `http://localhost:5006/enablebanking/callback`
- Sandbox uses the same API endpoint as production: `https://api.enablebanking.com`

No blockers.

---
*Phase: 01-foundation-and-api-client*
*Completed: 2026-02-18*
