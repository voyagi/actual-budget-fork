---
phase: 01-foundation-and-api-client
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - FOUND-04
gap_closure: true
must_haves:
  truths:
    - 'Every custom commit in the repo carries an [eb] prefix tag, including commit 371f06e2e which previously lacked it'
    - 'The feature branch feat/01-03-enablebanking-auth still contains all its commits and points to the same tree content after rebase'
  artifacts: []
  key_links: []
---

<objective>
Fix the one missing [eb] tag on commit 371f06e2e to fully satisfy FOUND-04 (fork commit convention).

Purpose: VERIFICATION.md identified that commit `371f06e2e` (`docs(01-02): complete Enable Banking sandbox registration plan`) is the only custom commit missing the `[eb]` tag. All other 9 custom commits carry it. This gap closure amends that commit message and rebases the feature branch so the full history is consistent.

Output: Clean git history where every custom commit carries `[eb]`. No file artifacts created - this is a git-only operation.
</objective>

<execution_context>
@C:/Users/Eagi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eagi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-api-client/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend commit 371f06e2e message and rebase feature branch</name>
  <files>None (git-only operation)</files>
  <action>
This is a git history rewrite on a personal fork. Force-push to master is acceptable per the planning context.

IMPORTANT: Do NOT use `git rebase -i` (interactive mode). MSYS bash cannot handle interactive terminal input. Use GIT_SEQUENCE_EDITOR or scripted approaches instead.

**Step 1: Record current state for safety**

- Note the current HEAD of `feat/01-03-enablebanking-auth` (should be `1125ad3cd`)
- Note current master HEAD (should be `462e2568e`)

**Step 2: Rewrite the commit on master**

- Check out master
- Use `git rebase` with `GIT_SEQUENCE_EDITOR` to non-interactively change commit `371f06e2e` from `pick` to `reword` (or use `edit`), then amend its message to: `docs(01-02): [eb] complete Enable Banking sandbox registration plan`

Concrete approach using GIT_SEQUENCE_EDITOR:

```bash
git checkout master
GIT_SEQUENCE_EDITOR="sed -i 's/^pick 371f06e/edit 371f06e/'" git rebase -i 371f06e2e~1
git commit --amend -m "docs(01-02): [eb] complete Enable Banking sandbox registration plan"
git rebase --continue
```

Alternative approach if sed-based rebase fails on MSYS: write a temporary script file that performs the sed replacement, then use it as GIT_SEQUENCE_EDITOR.

**Step 3: Force-push master**

```bash
git push --force origin master
```

**Step 4: Rebase feature branch onto new master**

```bash
git checkout feat/01-03-enablebanking-auth
git rebase master
```

**Step 5: Force-push feature branch**

```bash
git push --force origin feat/01-03-enablebanking-auth
```

**Step 6: Verify all commits carry [eb]**

- Run `git log --oneline` and confirm every custom commit (not upstream) has `[eb]` in its message
- Specifically confirm the previously-offending commit now reads `docs(01-02): [eb] complete Enable Banking sandbox registration plan`
  </action>
  <verify>
  Run `git log --oneline` on both master and feat/01-03-enablebanking-auth. Confirm:

1. The commit that was `371f06e2e` now has `[eb]` in its message (commit hash will have changed due to rewrite)
2. All custom commits (the ones after the upstream history) carry `[eb]`
3. The feature branch still has its commit (`docs(01-03): [eb] complete plan 01-03`)
4. No commits were lost (same number of custom commits as before)
5. Both master and feat/01-03-enablebanking-auth are force-pushed to origin
   </verify>
   <done>Every custom commit in the repository carries the [eb] tag. The previously-missing tag on the 01-02 documentation commit is now present. FOUND-04 is fully satisfied.</done>
   </task>

</tasks>

<verification>
After the git rewrite:
1. `git log --oneline master` shows all custom commits with [eb]
2. `git log --oneline feat/01-03-enablebanking-auth` shows all custom commits with [eb], including the feature branch commit
3. Both branches are pushed to origin (force-push acceptable on personal fork)
4. The tree content (actual files) is unchanged - only commit messages differ
</verification>

<success_criteria>

- FOUND-04 fully satisfied: 10/10 custom commits carry [eb] tag (was 9/10)
- No commits lost during rebase
- Both master and feature branch pushed to origin
- Phase 1 VERIFICATION.md gap is resolved
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-api-client/01-04-SUMMARY.md`
</output>
