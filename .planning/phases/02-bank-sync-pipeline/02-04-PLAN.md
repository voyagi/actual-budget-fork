---
phase: 02-bank-sync-pipeline
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - packages/desktop-client/src/enablebanking.ts
  - packages/desktop-client/src/hooks/useEnableBankingStatus.ts
  - packages/desktop-client/src/components/modals/EnableBankingExternalMsgModal.tsx
  - packages/desktop-client/src/components/Modals.tsx
  - packages/desktop-client/src/modals/modalsSlice.ts
  - packages/desktop-client/src/components/modals/SelectLinkedAccountsModal.tsx
  - packages/desktop-client/src/accounts/mutations.ts
  - packages/desktop-client/src/components/modals/CreateAccountModal.tsx
  - packages/loot-core/src/server/accounts/eb-category-rules.js
  - packages/loot-core/src/server/accounts/app.ts
autonomous: true
requirements: [SYNC-01, SYNC-02, SYNC-05, SYNC-06, SYNC-07, SYNC-09]

must_haves:
  truths:
    - "User sees 'Enable Banking' as a bank connection option in the Create Account modal"
    - "Clicking 'Enable Banking' opens an OAuth modal that redirects to the bank's login page"
    - "After OAuth completes, user sees their bank accounts and can link them to Actual accounts"
    - "Linked accounts have account_sync_source='enableBanking' and can be synced via the standard sync button"
    - "EU merchant categorization rules are seeded on first account link, gated by 'eb-rules-seeded' preference"
    - "Pending transactions appear uncleared (no checkmark) and booked transactions appear cleared"
    - "Last sync time is displayed for Enable Banking accounts via the existing AccountRow.tsx last_sync field, with detailed error info available via useEnableBankingSyncStatus hook"
  artifacts:
    - path: "packages/desktop-client/src/enablebanking.ts"
      provides: "authorizeEnableBank function that opens OAuth popup and polls for session"
      exports: ["authorizeEnableBank"]
    - path: "packages/desktop-client/src/hooks/useEnableBankingStatus.ts"
      provides: "Hook to check Enable Banking configuration status and per-account sync status (SYNC-07)"
      exports: ["useEnableBankingStatus", "useEnableBankingSyncStatus"]
    - path: "packages/desktop-client/src/components/modals/EnableBankingExternalMsgModal.tsx"
      provides: "Modal component for Enable Banking OAuth flow with country/bank selection"
      contains: "EnableBankingExternalMsgModal"
    - path: "packages/desktop-client/src/components/Modals.tsx"
      provides: "Registration of enablebanking-external-msg modal"
      contains: "enablebanking-external-msg"
    - path: "packages/desktop-client/src/modals/modalsSlice.ts"
      provides: "'enablebanking-external-msg' modal type in union, 'enableBanking' added to select-linked-accounts options"
      contains: "enablebanking-external-msg"
    - path: "packages/desktop-client/src/components/modals/SelectLinkedAccountsModal.tsx"
      provides: "'enableBanking' case in syncSource switch"
      contains: "enableBanking"
    - path: "packages/desktop-client/src/accounts/mutations.ts"
      provides: "useLinkAccountEnableBankingMutation hook"
      contains: "enablebanking-accounts-link"
    - path: "packages/desktop-client/src/components/modals/CreateAccountModal.tsx"
      provides: "Enable Banking entry in the bank connection options"
      contains: "enableBanking"
    - path: "packages/loot-core/src/server/accounts/eb-category-rules.js"
      provides: "EU_MERCHANT_PATTERNS array and seedCategoryRules function"
      exports: ["EU_MERCHANT_PATTERNS", "seedCategoryRules"]
  key_links:
    - from: "packages/desktop-client/src/enablebanking.ts"
      to: "loot-core IPC"
      via: "send('enablebanking-create-auth')"
      pattern: "send.*enablebanking-create-auth"
    - from: "packages/desktop-client/src/enablebanking.ts"
      to: "loot-core IPC"
      via: "send('enablebanking-poll-session') polling loop"
      pattern: "enablebanking-poll-session"
    - from: "packages/desktop-client/src/components/modals/EnableBankingExternalMsgModal.tsx"
      to: "packages/desktop-client/src/enablebanking.ts"
      via: "authorizeEnableBank call"
      pattern: "authorizeEnableBank"
    - from: "packages/desktop-client/src/accounts/mutations.ts"
      to: "loot-core IPC"
      via: "send('enablebanking-accounts-link')"
      pattern: "enablebanking-accounts-link"
    - from: "packages/loot-core/src/server/accounts/app.ts enablebanking-accounts-link"
      to: "packages/loot-core/src/server/accounts/eb-category-rules.js"
      via: "seedCategoryRules() call after account link"
      pattern: "seedCategoryRules"
---

<objective>
Build the desktop UI for Enable Banking: OAuth flow modal, account linking, and mutation hooks. Add Enable Banking as a connection option in the Create Account dialog. Create EU merchant categorization rules that seed on first account link.

Purpose: This makes Enable Banking usable by the end user. Without the UI, all the backend work from Plans 01-03 is unreachable. The category rules ensure imported transactions get automatically categorized for common EU merchants.

Output: 8 new/modified files in desktop-client, 2 files in loot-core
</objective>

<execution_context>
@C:/Users/Eagi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eagi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bank-sync-pipeline/02-RESEARCH.md
@.planning/phases/02-bank-sync-pipeline/02-03-SUMMARY.md
@packages/desktop-client/src/gocardless.ts
@packages/desktop-client/src/components/modals/GoCardlessExternalMsgModal.tsx
@packages/desktop-client/src/modals/modalsSlice.ts
@packages/desktop-client/src/components/modals/SelectLinkedAccountsModal.tsx
@packages/desktop-client/src/accounts/mutations.ts
@packages/desktop-client/src/components/modals/CreateAccountModal.tsx
@packages/desktop-client/src/components/Modals.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create enablebanking.ts, useEnableBankingStatus.ts, and EnableBankingExternalMsgModal.tsx</name>
  <files>
    packages/desktop-client/src/enablebanking.ts
    packages/desktop-client/src/hooks/useEnableBankingStatus.ts
    packages/desktop-client/src/components/modals/EnableBankingExternalMsgModal.tsx
  </files>
  <action>
    **enablebanking.ts** - Mirror `gocardless.ts` structure. Create `authorizeEnableBank(aspspName, aspspCountry)` async function:
    1. Call `send('enablebanking-create-auth', { aspspName, aspspCountry })` to get `{ url, state }`
    2. Open the OAuth URL: `window.Actual?.openURLInBrowser(url)` (or `window.open(url)` as fallback)
    3. Start polling loop: every 3 seconds, call `sendCatch('enablebanking-poll-session', { state })`
    4. When poll returns data with `accounts` array (non-empty), resolve with `{ accounts, state }`
    5. Timeout after 5 minutes, reject with error
    6. Export `authorizeEnableBank`

    Import `send` and `sendCatch` from `loot-core/src/platform/client/fetch` (or match the import path used in gocardless.ts).

    **useEnableBankingStatus.ts** - Mirror `useGoCardlessStatus.ts` (search for it in hooks/). Create TWO hooks:

    Hook 1: `useEnableBankingStatus()` - server configuration check:
    1. Calls `send('enablebanking-status')` on mount
    2. Returns `{ configured: boolean, loading: boolean }`
    3. Uses `useQuery` or `useState`/`useEffect` matching the GoCardless pattern

    Hook 2: `useEnableBankingSyncStatus(accountIds: string[])` - per-account sync status (SYNC-07):
    1. Calls `send('enablebanking-sync-status', { accountIds })` on mount and when accountIds change
    2. Returns `{ statuses: Record<string, { synced_at, status, error_message }>, loading: boolean }`
    3. This provides richer error details from `eb_sync_log` beyond the generic `last_sync` field
    4. NOTE: The existing `AccountRow.tsx` in `components/banksync/` already displays `last_sync` timestamp for ALL sync sources via the account entity. This hook supplements that with Enable Banking-specific error details.
    5. Export both hooks

    **EnableBankingExternalMsgModal.tsx** - Mirror `GoCardlessExternalMsgModal.tsx`. This is the modal that:
    1. Shows a country selector (text input or dropdown)
    2. On country select, calls `send('enablebanking-get-banks', { country })` to fetch available banks
    3. Displays bank list for user to pick
    4. On bank select, calls `authorizeEnableBank(aspspName, aspspCountry)`
    5. After OAuth completes (accounts returned from polling), dispatches `pushModal('select-linked-accounts', { accounts, syncSource: 'enableBanking' })`
    6. Shows loading states and error handling

    Follow the GoCardless modal's exact component structure, styling patterns, and modal API usage. Use the same UI components (Modal, Button, Input, etc.) imported from the Actual component library.

    IMPORTANT: Read `GoCardlessExternalMsgModal.tsx` carefully for the exact modal lifecycle (onMount, onClose, error handling, loading states). The Enable Banking version should feel identical to the user.
  </action>
  <verify>
    Read all 3 files. Confirm enablebanking.ts exports authorizeEnableBank with polling loop. Confirm useEnableBankingStatus.ts exports both `useEnableBankingStatus` (returns { configured, loading }) and `useEnableBankingSyncStatus` (returns { statuses, loading }). Confirm EnableBankingExternalMsgModal.tsx has country input, bank list, calls authorizeEnableBank, and dispatches select-linked-accounts modal on success.
  </verify>
  <done>
    Three new desktop-client files exist. The OAuth flow mirrors GoCardless: user selects country, picks bank, authorizes via redirect, and returns to see their accounts. The polling mechanism waits for the callback to complete before showing accounts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Enable Banking into existing modal system, SelectLinkedAccountsModal, mutations, and CreateAccountModal</name>
  <files>
    packages/desktop-client/src/components/Modals.tsx
    packages/desktop-client/src/modals/modalsSlice.ts
    packages/desktop-client/src/components/modals/SelectLinkedAccountsModal.tsx
    packages/desktop-client/src/accounts/mutations.ts
    packages/desktop-client/src/components/modals/CreateAccountModal.tsx
  </files>
  <action>
    **IMPORTANT: This task modifies 5 files. Read ALL 5 files before making any edits.** Each change is small (adding a case/entry to existing patterns), but reading first prevents context confusion. Process files in the order listed below.

    **modalsSlice.ts:**
    1. Add `'enablebanking-external-msg'` to the modal type union (search for `'gocardless-external-msg'` to find the exact location)
    2. Add `'enableBanking'` to the `syncSource` options in the `'select-linked-accounts'` modal type definition (search for `'goCardless'` in the select-linked-accounts union)
    3. If the modal options type includes account type definitions per syncSource, add `SyncServerEnableBankingAccount` (import from loot-core types)

    **Modals.tsx:**
    1. Import `EnableBankingExternalMsgModal` from the new file
    2. Add case for `'enablebanking-external-msg'` in the modal switch/map (search for `'gocardless-external-msg'` to find pattern)

    **SelectLinkedAccountsModal.tsx:**
    1. Find the `switch` on `syncSource` (or if/else chain)
    2. Add `'enableBanking'` case that mirrors the `'goCardless'` case but uses `useLinkAccountEnableBankingMutation` (created below) instead of the GoCardless mutation
    3. Import `SyncServerEnableBankingAccount` type if needed for type narrowing
    4. The Enable Banking case should display account name, IBAN (masked), and balance - same layout as GoCardless

    **mutations.ts:**
    1. Add `useLinkAccountEnableBankingMutation()` hook following the `useLinkAccountGoCardlessMutation` pattern (search for it)
    2. The mutation calls `send('enablebanking-accounts-link', { sessionId, account, upgradingId, offBudget, startingDate, startingBalance })`
    3. Export the new hook

    **CreateAccountModal.tsx:**
    1. Find where GoCardless or SimpleFin are listed as connection options (search for `gocardless-external-msg` or `simpleFin` in the file)
    2. Add an Enable Banking option: `{ name: 'Enable Banking', description: 'Connect EU bank accounts via Enable Banking (PSD2)', onClick: () => pushModal('enablebanking-external-msg') }` or match the exact pattern used by the other providers
    3. Place it after the existing bank connection options

    IMPORTANT: Search ALL switch/if statements on `syncSource` or `account_sync_source` across the desktop-client to find any other locations that need updating. TypeScript exhaustiveness checking will catch these at compile time if strict mode is enabled.
  </action>
  <verify>
    Read all 5 modified files. Grep for 'enablebanking-external-msg' in Modals.tsx and modalsSlice.ts. Grep for 'enableBanking' in SelectLinkedAccountsModal.tsx. Grep for 'enablebanking-accounts-link' in mutations.ts. Grep for 'Enable Banking' in CreateAccountModal.tsx. Run `yarn tsc --noEmit` in desktop-client to check for type errors (if feasible).
  </verify>
  <done>
    Enable Banking is fully integrated into the existing modal system. CreateAccountModal shows Enable Banking as a connection option. The OAuth flow modal chain works: external-msg -> select-linked-accounts. Account linking uses the correct mutation that calls the enablebanking-accounts-link IPC handler.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create EU merchant categorization rules and integrate into account link handler</name>
  <files>
    packages/loot-core/src/server/accounts/eb-category-rules.js
    packages/loot-core/src/server/accounts/app.ts
  </files>
  <action>
    **eb-category-rules.js** - Create new file with:

    1. `EU_MERCHANT_PATTERNS` constant array containing objects with `{ payeePattern: string, categoryName: string }`. Include at minimum:
       - Grocery chains: LIDL, ALDI, CARREFOUR, REWE, TESCO, SAINSBURY, KAUFLAND, PRISMA, K-MARKET, S-MARKET, ALBERT HEIJN, JUMBO, MIGROS, COOP (EU), MERCADONA, EROSKI, EDEKA, PENNY, NETTO
       - Subscriptions: NETFLIX, SPOTIFY, AMAZON PRIME, DISNEY+, YOUTUBE, HBO MAX, APPLE.COM
       - Transport: HSL, VR (Finnish rail), MATKAHUOLTO, TRANSDEV, FLIXBUS, UBER, BOLT, LIME, TIER
       - Utilities: FORTUM, HELEN, VATTENFALL, ELENIA, TELIA, DNA, ELISA
       - Shopping: AMAZON, ZALANDO, H&M, IKEA, TOKMANNI

    2. `seedCategoryRules()` async function (NO db parameter - imports its own):
       - Import `import * as db from '../db'` at the top of the file (this is the loot-core budget database, same as `packages/loot-core/src/server/db/index.ts`, NOT the sync-server account-db). The preferences, categories, and rules tables all live in this budget DB.
       - Check if already seeded: `db.first("SELECT value FROM preferences WHERE id = 'eb-rules-seeded'")`
       - If value is 'true', return early (already seeded)
       - For each pattern in EU_MERCHANT_PATTERNS:
         - Look up category by name: `db.first("SELECT id FROM categories WHERE name = ? AND tombstone = 0", [pattern.categoryName])`
         - If category not found, skip (this budget may not have that category)
         - Insert rule: `db.mutate("INSERT INTO rules (id, stage, conditionsOp, conditions, actions, tombstone) VALUES (?, null, 'and', ?, ?, 0)", [uuid.v4(), JSON.stringify([{ field: 'imported_payee', op: 'contains', value: pattern.payeePattern }]), JSON.stringify([{ field: 'category', op: 'set', value: category.id }])])`
       - Mark as seeded: `db.mutate("INSERT OR REPLACE INTO preferences (id, value) VALUES ('eb-rules-seeded', 'true')")`

    Use `uuid` from the loot-core uuid utility (search for how other files in loot-core generate UUIDs - likely `import * as uuid from '../../platform/server/uuid'` or similar).

    **app.ts** - Modify the `enablebanking-accounts-link` handler (created in Plan 02-03):
    - Import `seedCategoryRules` from `./eb-category-rules.js`
    - After the initial sync call and before returning 'ok', call `await seedCategoryRules()` (no arguments - it uses its own db import)
    - This ensures rules are seeded on first link, not on every sync
  </action>
  <verify>
    Read eb-category-rules.js and confirm: EU_MERCHANT_PATTERNS has 30+ entries across 5 categories. seedCategoryRules checks for 'eb-rules-seeded' preference before running. Rules use 'imported_payee' field with 'contains' operator. Read app.ts and confirm seedCategoryRules is called in the enablebanking-accounts-link handler.
  </verify>
  <done>
    EU merchant categorization rules are seeded once per budget on first Enable Banking account link. Rules cover grocery chains, subscriptions, transport, utilities, and shopping across major EU markets. The seeding is idempotent (gated by preference key). Categories are looked up by name at seed time (not hardcoded UUIDs).
  </done>
</task>

</tasks>

<verification>
1. Grep for 'enablebanking-external-msg' across desktop-client - should appear in Modals.tsx, modalsSlice.ts
2. Grep for 'enableBanking' across desktop-client - should appear in SelectLinkedAccountsModal, mutations, CreateAccountModal, modalsSlice
3. Grep for 'authorizeEnableBank' in enablebanking.ts and EnableBankingExternalMsgModal.tsx
4. Grep for 'seedCategoryRules' in eb-category-rules.js and app.ts
5. EU_MERCHANT_PATTERNS has entries for all 5 categories (Groceries, Entertainment, Transport, Utilities, Shopping)
6. `yarn tsc --noEmit` passes in desktop-client and loot-core (or no NEW type errors)
</verification>

<success_criteria>
A user can: (1) see "Enable Banking" as a connection option in Create Account, (2) click it and select their country and bank, (3) authorize via OAuth redirect, (4) see their bank accounts listed for linking, (5) link an account to an Actual account, (6) have EU merchant rules auto-seeded for categorization. The pending/booked visual distinction works automatically through the existing cleared field.
</success_criteria>

<output>
After completion, create `.planning/phases/02-bank-sync-pipeline/02-04-SUMMARY.md`
</output>
