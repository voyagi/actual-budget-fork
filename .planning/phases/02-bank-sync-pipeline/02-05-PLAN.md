---
phase: 02-bank-sync-pipeline
plan: 05
type: execute
wave: 4
depends_on: [02-04]
files_modified: []
autonomous: false
requirements:
  [
    SYNC-01,
    SYNC-02,
    SYNC-03,
    SYNC-04,
    SYNC-05,
    SYNC-06,
    SYNC-07,
    SYNC-08,
    SYNC-09,
  ]

must_haves:
  truths:
    - 'Docker image builds successfully with all Phase 2 code'
    - 'App starts and Enable Banking shows as configured'
    - 'OAuth flow completes against sandbox bank'
    - 'Transactions import correctly with proper amounts and dates'
    - 'Pending and booked transactions display differently in the ledger'
    - 'Second bank connection does not break the first'
    - 'Sync log entries exist in eb_sync_log table'
    - 'Category rules are seeded after first link'
  artifacts: []
  key_links: []
---

<objective>
Rebuild Docker, verify the complete Enable Banking pipeline end-to-end against the sandbox, and get human verification that the flow works correctly from the user's perspective.

Purpose: All code is written but untested as an integrated system. This plan proves the full pipeline works: Docker build, OAuth flow, account linking, transaction import, deduplication, balance update, pending/booked display, multi-session support, sync logging, and category rules.

Output: Verified working system, ready for Phase 3 (automation)
</objective>

<execution_context>
@C:/Users/Eagi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eagi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bank-sync-pipeline/02-RESEARCH.md
@.planning/phases/02-bank-sync-pipeline/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker rebuild and automated smoke tests</name>
  <files></files>
  <action>
    1. Run `docker compose build --no-cache` to rebuild with all Phase 2 code
    2. Run `docker compose up -d` to start the stack
    3. Wait for healthy startup (check `docker compose logs --tail=20`)
    4. Verify Enable Banking is configured: call `GET http://localhost:5006/enablebanking/test-auth` (use PowerShell Invoke-WebRequest, not curl). Expect `{"status":"ok","data":{"configured":true}}`
    5. Verify migrations ran: call `POST http://localhost:5006/enablebanking/get-banks` with a valid session token and `{ "country": "FI" }`. If this returns banks, the migrations and service layer work. If 401, that is expected (session auth required) - confirm the route exists by checking logs.
    6. Check that `yarn tsc --noEmit` passes inside the container or locally for loot-core and desktop-client (if feasible in the Docker context). Alternatively, confirm the Vite build succeeded by checking that the app loads in the browser at `http://localhost:5006`.
    7. Open `http://localhost:5006` in the browser and confirm the app loads. Navigate to add account and verify "Enable Banking" appears as a connection option.

    If any step fails, fix the issue (likely import paths, missing exports, or TypeScript errors) and rebuild.

  </action>
  <verify>
    Docker compose up succeeds. test-auth returns configured:true. App loads in browser. Enable Banking option visible in Create Account.
  </verify>
  <done>
    Docker image builds and runs with all Phase 2 code. Enable Banking shows as configured. The app UI shows Enable Banking as a connection option.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of end-to-end Enable Banking flow</name>
  <files></files>
  <action>
    **What was built:** Complete Enable Banking integration from Plans 02-01 through 02-04: OAuth flow, account linking, transaction import with deduplication, balance update, pending/booked display, multi-session support, sync logging, and EU merchant categorization rules.

    **How to verify - run these 8 tests:**

    **Test 1: OAuth Flow (SYNC-01)**
    1. Open http://localhost:5006 in Chrome
    2. Create or open a budget
    3. Click "Add Account" (or "Link bank" if that is the button label)
    4. Select "Enable Banking"
    5. Select country: Finland (FI) - or whatever sandbox supports
    6. Select a sandbox test bank from the list
    7. Complete the bank's login flow (sandbox credentials)
    8. Verify: You return to the app and see bank accounts listed

    **Test 2: Account Linking (SYNC-02)**
    1. In the accounts list from Test 1, select one account
    2. Map it to a new Actual account (or link to existing)
    3. Verify: Account appears in the sidebar with the bank name
    4. Verify: Initial sync runs and transactions appear in the ledger

    **Test 3: Deduplication (SYNC-03)**
    1. Click the sync button on the linked account
    2. Verify: No duplicate transactions created
    3. Check transaction count before and after - should be identical

    **Test 4: Balance (SYNC-04)**
    1. Check the account balance in Actual
    2. Compare with the balance shown during account linking
    3. Verify: Balances match (or are close - pending transactions may cause small differences)

    **Test 5: Pending vs Booked (SYNC-05)**
    1. Look at imported transactions in the ledger
    2. Verify: Some transactions show as cleared (checkmark) and some as uncleared
    3. Uncleared = pending (PDNG), Cleared = booked (BOOK)

    **Test 6: Multi-session (SYNC-06)**
    1. Repeat Test 1 with a DIFFERENT sandbox bank
    2. Link a second account
    3. Verify: First account still syncs correctly
    4. Verify: Second account also syncs

    **Test 7: Sync Status (SYNC-07)**
    1. After syncing, navigate to the Bank Sync page (or account view)
    2. Verify the last sync time is displayed for the Enable Banking account (the existing AccountRow.tsx shows `last_sync` for all sync sources)
    3. The `useEnableBankingSyncStatus` hook provides additional error details from `eb_sync_log` if needed

    **Test 8: Category Rules (SYNC-09)**
    1. Check if any imported transactions have auto-assigned categories
    2. (If sandbox transactions do not match EU merchants, check that rules exist in the rules table)

    If any test fails, describe the issue and I will fix it.

  </action>
  <verify>
    All 8 tests pass. User confirms OAuth flow, account linking, transaction import, deduplication, balance update, pending/booked display, multi-session, and category rules all work.
  </verify>
  <done>
    User has verified the complete Enable Banking pipeline end-to-end against the sandbox. Type "approved" to complete Phase 2, or describe any issues found.
  </done>
</task>

</tasks>

<verification>
All 9 SYNC requirements verified through the human testing protocol above.
</verification>

<success_criteria>
User confirms: OAuth flow works, accounts link, transactions import without duplicates, balances are correct, pending/booked display works, multiple banks can be connected, sync logging works, and category rules are seeded.
</success_criteria>

<output>
After completion, create `.planning/phases/02-bank-sync-pipeline/02-05-SUMMARY.md`
</output>
