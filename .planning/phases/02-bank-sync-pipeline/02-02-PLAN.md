---
phase: 02-bank-sync-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/loot-core/src/types/models/account.ts
  - packages/loot-core/src/server/server-config.ts
  - packages/loot-core/src/server/accounts/sync.ts
autonomous: true
requirements: [SYNC-03, SYNC-04]

must_haves:
  truths:
    - "'enableBanking' is a valid AccountSyncSource in the type system"
    - "syncAccount() has an 'enableBanking' branch that calls downloadEnableBankingTransactions()"
    - "downloadEnableBankingTransactions() POSTs to ENABLEBANKING_SERVER/transactions and returns { transactions, accountBalance, startingBalance }"
    - "ENABLEBANKING_SERVER resolves to the sync-server /enablebanking path"
  artifacts:
    - path: "packages/loot-core/src/types/models/account.ts"
      provides: "'enableBanking' in AccountSyncSource union, SyncServerEnableBankingAccount type"
      contains: "enableBanking"
    - path: "packages/loot-core/src/server/server-config.ts"
      provides: "ENABLEBANKING_SERVER in ServerConfig type and getServer() return"
      contains: "ENABLEBANKING_SERVER"
    - path: "packages/loot-core/src/server/accounts/sync.ts"
      provides: "downloadEnableBankingTransactions function, 'enableBanking' branch in syncAccount"
      contains: "downloadEnableBankingTransactions"
  key_links:
    - from: "packages/loot-core/src/server/accounts/sync.ts"
      to: "packages/sync-server/src/app-enablebanking/app-enablebanking.js"
      via: "HTTP POST to /enablebanking/transactions"
      pattern: "ENABLEBANKING_SERVER.*transactions"
    - from: "packages/loot-core/src/server/accounts/sync.ts"
      to: "packages/loot-core/src/server/accounts/sync.ts processBankSyncDownload"
      via: "download result passed to shared pipeline"
      pattern: "processBankSyncDownload"
---

<objective>
Add Enable Banking as a recognized sync source in loot-core: extend the TypeScript type union, add the server config endpoint, and implement the download function that will be called during manual sync to fetch transactions from the sync-server.

Purpose: This is the loot-core half of the sync pipeline. The download function bridges loot-core (running in a browser web worker) to the sync-server (running in Node.js). Without this, the existing sync button cannot trigger Enable Banking transaction fetches.

Output: 3 modified files in packages/loot-core/
</objective>

<execution_context>
@C:/Users/Eagi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eagi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bank-sync-pipeline/02-RESEARCH.md
@packages/loot-core/src/types/models/account.ts
@packages/loot-core/src/server/server-config.ts
@packages/loot-core/src/server/accounts/sync.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'enableBanking' to AccountSyncSource and create SyncServerEnableBankingAccount type</name>
  <files>
    packages/loot-core/src/types/models/account.ts
  </files>
  <action>
    Modify `account.ts` to make two changes:

    1. Extend the `AccountSyncSource` union type to include `'enableBanking'`:
       `export type AccountSyncSource = 'simpleFin' | 'goCardless' | 'pluggyai' | 'enableBanking';`

    2. Add the `SyncServerEnableBankingAccount` type export (mirrors `SyncServerGoCardlessAccount` pattern - search for that type to find the exact structure). The Enable Banking version:
       ```
       export type SyncServerEnableBankingAccount = {
         account_id: string;
         name: string;
         institution: string;
         mask: string;
         official_name: string | null;
         balance: number | null;
         iban: string | null;
         session_id: string;
       };
       ```

    Search the codebase for all files that import `AccountSyncSource` or `SyncServerGoCardlessAccount` to verify no other files need updating for the type change. The TypeScript compiler will catch missing switch cases.
  </action>
  <verify>
    Read `account.ts` and confirm `'enableBanking'` is in the union. Grep for `AccountSyncSource` across the codebase to verify no import breaks. Grep for `SyncServerEnableBankingAccount` to confirm it is exported.
  </verify>
  <done>
    `AccountSyncSource` includes `'enableBanking'`. `SyncServerEnableBankingAccount` type is exported and matches the shape needed by the SelectLinkedAccountsModal and mutations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ENABLEBANKING_SERVER to server-config.ts and downloadEnableBankingTransactions to sync.ts</name>
  <files>
    packages/loot-core/src/server/server-config.ts
    packages/loot-core/src/server/accounts/sync.ts
  </files>
  <action>
    **server-config.ts:**
    1. Add `ENABLEBANKING_SERVER: string;` to the `ServerConfig` type
    2. Add `ENABLEBANKING_SERVER: joinURL(url, '/enablebanking'),` to the return object in `getServer()`, after the `PLUGGYAI_SERVER` line

    **sync.ts:**
    1. Add `downloadEnableBankingTransactions()` async function. Follow the exact pattern of `downloadGoCardlessTransactions()` (located around line 131). The Enable Banking version is simpler - it only needs `acctId` and `since` parameters:

       ```
       async function downloadEnableBankingTransactions(
         acctId: string,
         since: string,
       ) {
         const userToken = await asyncStorage.getItem('user-token');
         if (!userToken) return;

         const res = await post(
           getServer().ENABLEBANKING_SERVER + '/transactions',
           { accountId: acctId, startDate: since },
           { 'X-ACTUAL-TOKEN': userToken },
           60000,
         );

         if (res.error_code) {
           throw BankSyncError(res.error_type, res.error_code);
         }

         return {
           transactions: res.transactions.all,
           accountBalance: res.balances,
           startingBalance: res.startingBalance,
         };
       }
       ```

       Place it after the existing `downloadPluggyAiTransactions` function.

    2. Add the `'enableBanking'` branch in `syncAccount()`. Find the `else if (acctRow.account_sync_source === 'pluggyai')` block (around line 1038) and add after it, BEFORE the `else { throw }` block:

       ```
       } else if (acctRow.account_sync_source === 'enableBanking') {
         download = await downloadEnableBankingTransactions(acctId, syncStartDate);
       ```

    Verify by reading the `syncAccount` function that the new branch is in the correct position (before the final `else` that throws "Unrecognized bank-sync provider").

    IMPORTANT: Check that the `post` helper, `asyncStorage`, `getServer`, `BankSyncError`, and `logger` are already imported at the top of sync.ts (they should be - used by existing download functions). Do NOT add duplicate imports.
  </action>
  <verify>
    Read server-config.ts to confirm ENABLEBANKING_SERVER in both the type and the getServer() return. Read sync.ts to confirm: (1) downloadEnableBankingTransactions exists and POSTs to ENABLEBANKING_SERVER + '/transactions', (2) syncAccount has 'enableBanking' branch before the throw, (3) no duplicate imports added.
  </verify>
  <done>
    server-config.ts resolves ENABLEBANKING_SERVER to /enablebanking. sync.ts has downloadEnableBankingTransactions that POSTs accountId + startDate to the transactions route and returns { transactions, accountBalance, startingBalance }. syncAccount() routes 'enableBanking' accounts to the new download function. The download result feeds into the existing processBankSyncDownload() pipeline unchanged.
  </done>
</task>

</tasks>

<verification>
1. `yarn tsc --noEmit` in packages/loot-core passes (or at minimum, no NEW errors from our changes - check diff against baseline)
2. grep confirms 'enableBanking' appears in AccountSyncSource
3. grep confirms ENABLEBANKING_SERVER appears in ServerConfig type and getServer()
4. grep confirms downloadEnableBankingTransactions exists in sync.ts
5. grep confirms 'enableBanking' branch exists in syncAccount()
</verification>

<success_criteria>
loot-core recognizes 'enableBanking' as a valid sync source at the type level and can route sync requests through the new download function to the sync-server /enablebanking/transactions endpoint. The existing processBankSyncDownload/reconcileTransactions pipeline processes the result without modification.
</success_criteria>

<output>
After completion, create `.planning/phases/02-bank-sync-pipeline/02-02-SUMMARY.md`
</output>
